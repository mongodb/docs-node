=================
Change a Document
=================

.. default-domain:: mongodb

Overview
--------

You can alter documents in MongoDB in two different ways.

Update operations require a description of how to change matched
documents. The update document gives us a way of communicating such a
description.




link to $set etc. operators
talk about explicit value setting

.. _updateDocuments:

Update Documents
----------------

Use `update operators
<https://docs.mongodb.com/manual/reference/operator/update/>`_ to alter
matched documents.


.. _replacementDocuments:

Replacement Documents
---------------------

Methods that replace, rather than update, documents accept a
**replacement document**. This document consists of fields with literal
values that correspond one-to-one with the fields and values in the
resulting altered document. For instance, consider a collection
containing the 
